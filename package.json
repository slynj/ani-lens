{
  "name": "ani-lens",
  "version": "1.0.0",
  "description": "",
  "main": "webpack.config.js",
  "scripts": {
    "start": "nvm use 16 && react-scripts --openssl-legacy-provider start",
    "build": "nvm use 16 && react-scripts --openssl-legacy-provider build",
    "test": "nvm use 16 && react-scripts test",
    "eject": "react-scripts eject"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "process": "^0.11.10"
  },
  "devDependencies": {},
  "engines": {
    "node": "16"
  }
}
